export default function BrainBreak(brainBreak) {
    return `
      <div class="container-fluid api-button text-center">    
      <h1>${brainBreak.activity}</h1>
      <h1>${brainBreak.link}</h1>
      </div>`;
  }