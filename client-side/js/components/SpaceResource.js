export default function SpaceResource(spaceResource) {
    return `
    <img src= ${wilsonId.collection.items[1].links[0].href}></img>
    <h1>${wilsonId.collection.items[0].data[0].title}</h1>
    <h2>${wilsonId.collection.items[0].data[0].description}</h2>   
    <button class="returnScience">Return to Science</button>
    `;
}
